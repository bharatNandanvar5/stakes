export enum GameType {
  MINES = 'mines',
  TIC_TAC_TOE = 'tictactoe',
  SCRIBBLE = 'scribble',
}

export enum GameState {
  FINISHED = 'finished',
  PLAYING = 'playing',
  ENDED = 'ended',
  WAITING = 'waiting',
}
